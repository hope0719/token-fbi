# 2026-07-31 · Token FBI 每日情报更新

## 本次新增（2 张卡，共 52 → 54 张）

均判定为「新模型档」，插入 TOKENS 数组前部（固定置顶 StepFun 之后、AtomCode 之前）。

1. **360 纳米 Work** · 工具 · 4 火
   - 额度：首批入驻用户每人 1 亿 Token 免费试用（优先扶持 1000 家中小企业，名额有限）
   - 2026-07-28 周鸿祎发布的企业多智能体协同办公平台（原「安全龙虾」升级），约 500 个岗位 AI 专家、5 万+ 技能、云端 7×24 定时任务、三层知识库；自动调度 Kimi/DeepSeek/GLM/豆包等 16 家模型
   - 入口：https://work.n.cn（网页 / Windows / 移动端）

2. **BazaarLink** · 大模型 · 4 火
   - 额度：DeepSeek V4 Flash 长期免费，10 RPM / 150 次每天，不绑卡不充值（充值账户 ×3 至 450 次/天）
   - OpenAI 完全兼容聚合网关，1M 上下文；支持 `model="auto:free"` 零成本智能路由；Agent 可 POST `/api/v1/agents/register` 自助领 Key
   - base_url：`https://bazaarlink.ai/api/v1`，模型名 `deepseek/deepseek-v4-flash`
   - 入口：https://bazaarlink.ai/free

## 已排查但未收录
- **香港 3HK 免费送 AI Tokens（1000 万）**：需新上台/续约指定 5G 月费计划才可领，属电信套餐捆绑权益，非零门槛免费，不收录。
- **ai.amanai.dev（10M 注册送 + 每日 1M）**：仅见境外论坛帖，官方无公开说明、评论区大量「不可用」反馈，来源不可靠，暂不收录。
- LongCat 每日 10 万免费 token：已由现有「美团 longcat 大模型」卡覆盖，未重复新增。

## 站点发布
- `node --check app.js` 通过；校验 54 张卡、字段无缺失、无重名（京东 JoyAgent 的 `rating: 0` 是故意标注「热度记作零」，非缺字段）。
- commit `0d46973`，已推送 origin/main。
- 站点验证：`hope0719.github.io/token-fbi/` 首页 200，`app.js` 200 且含新卡内容。

## 环境坑点（重要）
- 自动化 cwd `/Users/hope/Desktop/token 无限/token-fbi` 仍是空壳（只有 .workbuddy），**真实仓库在 `/Users/hope/Desktop/个人作品集/token 无限/token-fbi/`**，git 操作须切到该路径。
- 本机 **无代理进程监听 7890**，但 git 全局配置了 `socks5://127.0.0.1:7890`，且 GitHub 443 直连会报 `LibreSSL SSL_connect: SSL_ERROR_SYSCALL`。
- **可用推送姿势**：`git -c http.proxy= -c https.proxy= -c http.version=HTTP/1.1 push origin main` —— 加上 `http.version=HTTP/1.1` 后一次成功，仅清空代理不加该参数会失败。

## 待关注（下次执行时复核）
- 今日（7/31）到期：阶跃星辰 StepFun、阿里 qoderwork 每日 200 次限免、无问AI SecurityPlan —— 明日需确认是否延期或下架。
- 8/2 到期：天翼云息壤。
- SCNet.AI 的 `limited: 2026-07-23` 已过期 8 天，卡片仍在展示过期角标，建议下次核实后修正或下架。
